rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /live_thoughts/{doc}   { allow read, write: if true; }
    match /anon_questions/{doc}  { allow read, write: if true; }
    match /{document=**}         { allow read, write: if false; }
  }
}
